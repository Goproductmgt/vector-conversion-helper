{
  "name": "workspace",
  "version": "1.0.0",
  "description": "A backend API that converts raster images (JPG, PNG, HEIC) to vector formats (SVG, EPS, PDF) for print shops.",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lucide-react": "^0.562.0"
  }
}
